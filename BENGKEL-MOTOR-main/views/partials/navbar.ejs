<nav class="navbar navbar-expand-lg navbar-dark bg-dark shadow-sm py-2">
    <div class="container">
        <a class="navbar-brand fw-bold d-flex align-items-center" href="<%= (typeof user !== 'undefined' && user) ? '/dashboard-redirect' : '/' %>">
            <div class="bg-success text-white rounded-3 p-1 me-2 d-flex align-items-center justify-content-center" style="width: 32px; height: 32px;">
                <i class="fas fa-screwdriver-wrench"></i>
            </div>
            <span>ACR Motor</span>
        </a>

        <div class="d-flex align-items-center">
            <ul class="nav flex-row align-items-center">
                <li class="nav-item">
                    <a class="nav-link px-2 text-white position-relative <%= (activePage === 'home') ? 'fw-bold active-highlight' : 'opacity-75' %>" 
                       href="<%= (typeof user !== 'undefined' && user) ? '/dashboard-redirect' : '/' %>">
                       Overview
                    </a>
                </li>
                <li class="nav-item border-start border-secondary ms-2 ps-2">
                    <a class="nav-link px-2 text-white position-relative <%= (activePage === 'about') ? 'fw-bold active-highlight' : 'opacity-75' %>" 
                       href="/about">
                       About
                    </a>
                </li>
            </ul>
        </div>

        <% if (typeof user !== 'undefined' && user) { %>
            <button class="navbar-toggler border-0 shadow-none ms-auto" type="button" data-bs-toggle="collapse" data-bs-target="#navInternal">
                <span class="navbar-toggler-icon"></span>
            </button>
        <% } %>

        <div class="collapse navbar-collapse" id="navInternal">
            <ul class="navbar-nav ms-auto align-items-lg-center">
                <% if (typeof user !== 'undefined' && user) { %>
                    
                    <% if (user.role === 'admin' || user.role === 'kasir') { %>
                        <li class="nav-item"><a class="nav-link small hover-link" href="/parts"><i class="fas fa-box me-1"></i> Suku Cadang</a></li>
                        <li class="nav-item"><a class="nav-link small hover-link" href="/services"><i class="fas fa-wrench me-1"></i> Jasa</a></li>
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle small" href="#" data-bs-toggle="dropdown">Transaksi</a>
                            <ul class="dropdown-menu dropdown-menu-dark shadow-lg border-secondary animate-slide-in">
                                <li><a class="dropdown-item small" href="/transactions/add"><i class="fas fa-plus-circle me-2"></i>Baru</a></li>
                                <li><a class="dropdown-item small" href="/transactions"><i class="fas fa-history me-2"></i>Riwayat</a></li>
                            </ul>
                        </li>
                    <% } %>

                    <% if (user.role === 'mekanik') { %>
                        <li class="nav-item">
                            <a class="nav-link small hover-link" href="/api/jobs/mechanic/dashboard"><i class="fas fa-tasks me-1"></i>Tugas Saya</a>
                        </li>
                    <% } %>

                    <li class="nav-item dropdown ms-lg-3 mt-3 mt-lg-0 border-top border-secondary pt-3 pt-lg-0">
                        <a class="nav-link dropdown-toggle d-flex align-items-center profile-pill px-3 py-1 rounded-pill" 
                           href="#" role="button" data-bs-toggle="dropdown">
                            
                            <% if (user.profileImage && user.profileImage !== '/img/default-avatar.png') { %>
                                <img src="<%= user.profileImage %>" class="rounded-circle me-2 border border-2 border-success shadow-sm" style="width: 28px; height: 28px; object-fit: cover;">
                            <% } else { %>
                                <div class="bg-success text-white rounded-circle d-flex align-items-center justify-content-center me-2 shadow-sm" style="width: 28px; height: 28px; font-weight: bold; font-size: 0.75rem;">
                                    <%= user.name.charAt(0).toUpperCase() %>
                                </div>
                            <% } %>
                            
                            <div class="d-flex flex-column text-start me-1">
                                <span class="small fw-bold text-white lh-1"><%= user.name.split(' ')[0] %></span>
                                <span class="text-muted" style="font-size: 0.6rem;"><%= user.role.toUpperCase() %></span>
                            </div>
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end dropdown-menu-dark shadow-lg border-secondary rounded-3 animate-slide-in">
                            <li><a class="dropdown-item small" href="/users/edit/<%= user._id %>"><i class="fas fa-user-edit me-2"></i>Edit Profil</a></li>
                            <li><hr class="dropdown-divider border-secondary"></li>
                            <li><a class="dropdown-item small text-danger fw-bold" href="/logout"><i class="fas fa-sign-out-alt me-2"></i>Keluar</a></li>
                        </ul>
                    </li>
                <% } else { %>
                    <li class="nav-item ms-lg-3 mt-3 mt-lg-0">
                        <a href="/login" class="btn btn-success btn-sm px-4 rounded-pill fw-bold shadow-sm btn-hover-grow">
                            <i class="fas fa-sign-in-alt me-1"></i>Masuk
                        </a>
                    </li>
                <% } %>
            </ul>
        </div>
    </div>
</nav>

<style>
    /* 1. Efek Highlight Garis Bawah */
    .active-highlight::after {
        content: "";
        position: absolute;
        bottom: -2px;
        left: 8px;
        right: 8px;
        height: 3px;
        background: #2ecc71;
        border-radius: 10px;
        animation: expand 0.3s ease-out;
    }

    @keyframes expand {
        from { width: 0; left: 50%; }
        to { width: auto; left: 8px; }
    }

    /* 2. Animasi Dropdown Slide-In */
    .animate-slide-in {
        animation: slideIn 0.2s ease-out;
    }

    @keyframes slideIn {
        from { opacity: 0; transform: translateY(10px); }
        to { opacity: 1; transform: translateY(0); }
    }

    /* 3. Perbaikan Visual Profile Pill */
    .profile-pill {
        background: rgba(255, 255, 255, 0.05);
        transition: all 0.3s ease;
        border: 1px solid rgba(255, 255, 255, 0.1);
    }

    .profile-pill:hover {
        background: rgba(46, 204, 113, 0.15);
        border-color: rgba(46, 204, 113, 0.4);
    }

    .hover-link {
        transition: color 0.3s ease;
    }

    .hover-link:hover {
        color: #2ecc71 !important;
    }

    .btn-hover-grow {
        transition: transform 0.2s ease;
    }

    .btn-hover-grow:hover {
        transform: scale(1.05);
    }
</style>