<div class="container-fluid px-4 py-4 animate-fade-in">
    <div class="d-flex flex-column flex-md-row justify-content-between align-items-md-center mb-4 gap-3">
        <div>
            <h1 class="h3 fw-bold text-dark mb-1">Daftar Jasa Servis</h1>
            <p class="text-muted mb-0">Kelola katalog layanan jasa dan estimasi biaya pengerjaan bengkel.</p>
        </div>
        <div>
            <a href="/services/add" class="btn btn-success px-4 py-2 fw-bold rounded-3 shadow-sm btn-emerald">
                <i class="fas fa-plus-circle me-2"></i>Tambah Jasa Baru
            </a>
        </div>
    </div>

    <div class="row g-3 mb-4">
        <div class="col-md-6">
            <div class="card border-0 shadow-sm rounded-4 p-3 bg-white border-start border-4 border-primary">
                <div class="d-flex align-items-center">
                    <div class="icon-box bg-primary bg-opacity-10 text-primary rounded-3 p-3 me-3">
                        <i class="fas fa-hand-holding-medical fa-xl"></i>
                    </div>
                    <div>
                        <h6 class="text-muted small mb-1">Total Jenis Layanan</h6>
                        <h4 class="fw-bold mb-0"><%= services.length %> <span class="small fw-normal text-muted">Item</span></h4>
                    </div>
                </div>
            </div>
        </div>
        <div class="col-md-6">
            <div class="card border-0 shadow-sm rounded-4 p-3 bg-white border-start border-4 border-info">
                <div class="d-flex align-items-center">
                    <div class="icon-box bg-info bg-opacity-10 text-info rounded-3 p-3 me-3">
                        <i class="fas fa-tags fa-xl"></i>
                    </div>
                    <div>
                        <h6 class="text-muted small mb-1">Rata-rata Biaya Jasa</h6>
                        <% 
                            let totalServicePrice = services.reduce((acc, s) => acc + (s.price || 0), 0);
                            let avgPrice = services.length > 0 ? totalServicePrice / services.length : 0;
                        %>
                        <h4 class="fw-bold mb-0">Rp <%= Math.round(avgPrice).toLocaleString('id-ID') %></h4>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div class="card border-0 shadow-sm rounded-4 overflow-hidden">
        <div class="card-header bg-white py-3 border-bottom border-light">
            <div class="row align-items-center">
                <div class="col">
                    <h6 class="mb-0 fw-bold"><i class="fas fa-list-ul me-2 text-success"></i>Katalog Layanan</h6>
                </div>
                <div class="col-auto">
                    <div class="input-group input-group-sm shadow-sm rounded-pill overflow-hidden border">
                        <span class="input-group-text bg-white border-0"><i class="fas fa-search text-muted"></i></span>
                        <input type="text" id="serviceSearch" class="form-control border-0 ps-0" placeholder="Cari jasa atau kode...">
                    </div>
                </div>
            </div>
        </div>
        <div class="card-body p-0">
            <% if (services.length > 0) { %>
                <div class="table-responsive">
                    <table class="table table-hover align-middle mb-0" id="serviceTable">
                        <thead class="table-light">
                            <tr class="small text-uppercase fw-bold text-muted">
                                <th class="ps-4">Kode Jasa</th>
                                <th>Nama Layanan</th>
                                <th>Biaya Layanan</th>
                                <th class="text-center pe-4">Aksi</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% services.forEach(srv => { %>
                                <tr>
                                    <td class="ps-4">
                                        <span class="badge bg-light text-dark border font-monospace px-2 py-1">
                                            <%= srv.code %>
                                        </span>
                                    </td>
                                    <td>
                                        <div class="fw-bold text-dark"><%= srv.name %></div>
                                        <small class="text-muted">Verified Repair Service</small>
                                    </td>
                                    <td>
                                        <div class="fw-bold text-success">
                                            Rp <%= srv.price.toLocaleString('id-ID') %>
                                        </div>
                                    </td>
                                    <td class="text-center pe-4">
                                        <div class="btn-group shadow-sm rounded-3">
                                            <a href="/services/edit/<%= srv._id %>" class="btn btn-sm btn-white border border-light" title="Edit Jasa">
                                                <i class="fas fa-edit text-warning"></i>
                                            </a>
                                            <form action="/api/services/delete/<%= srv._id %>?_method=DELETE" method="POST" class="d-inline">
                                                <button type="submit" class="btn btn-sm btn-white border border-light" 
                                                    onclick="return confirm('Hapus layanan jasa ini?');">
                                                    <i class="fas fa-trash-alt text-danger"></i>
                                                </button>
                                            </form>
                                        </div>
                                    </td>
                                </tr>
                            <% }); %>
                        </tbody>
                    </table>
                </div>
            <% } else { %>
                <div class="text-center py-5">
                    <i class="fas fa-concierge-bell fa-4x text-muted opacity-25 mb-3"></i>
                    <h5 class="text-muted fw-bold">Belum Ada Layanan Jasa</h5>
                    <p class="small text-muted">Silakan tambahkan jasa servis baru untuk memulai operasional.</p>
                </div>
            <% } %>
        </div>
    </div>
</div>

<style>
    .btn-emerald { background-color: #2ecc71; border: none; transition: 0.3s; color: white; }
    .btn-emerald:hover { background-color: #27ae60; transform: translateY(-2px); box-shadow: 0 5px 15px rgba(46, 204, 113, 0.3); color: white; }
    
    .btn-white { background: #fff; }
    .btn-white:hover { background: #f8f9fa; }

    .font-monospace { font-family: 'Courier New', Courier, monospace; letter-spacing: 1px; }
    
    .animate-fade-in { animation: fadeIn 0.5s ease-out; }
    @keyframes fadeIn { from { opacity: 0; transform: translateY(10px); } to { opacity: 1; transform: translateY(0); } }

    #serviceTable tbody tr:hover { background-color: #f8fffb; }
    .icon-box { width: 50px; height: 50px; display: flex; align-items: center; justify-content: center; }
</style>

<script>
    // Pencarian Jasa Real-time
    document.getElementById('serviceSearch').addEventListener('keyup', function() {
        let value = this.value.toLowerCase();
        let rows = document.querySelectorAll("#serviceTable tbody tr");
        
        rows.forEach(row => {
            let text = row.innerText.toLowerCase();
            row.style.display = text.includes(value) ? "" : "none";
        });
    });
</script>