<div class="container py-5 animate-fade-in">
    <div class="row justify-content-center">
        <div class="col-md-10 col-lg-7">
            <div class="card shadow-lg border-0 rounded-4 overflow-hidden">
                <div class="card-header bg-emerald-gradient text-white py-4 border-0 text-center">
                    <div class="mb-2">
                        <i class="fas fa-hand-sparkles fa-2x"></i>
                    </div>
                    <h3 class="mb-1 fw-bold">Edit Layanan Jasa</h3>
                    <p class="mb-0 opacity-75 small">Memperbarui: <strong><%= service.name %></strong></p>
                </div>

                <div class="card-body p-4 p-md-5 bg-white">
                    <form action="/api/services/<%= service._id %>?_method=PUT" method="POST">
                        
                        <div class="row g-4 mb-4">
                            <div class="col-md-8">
                                <label class="form-label small fw-bold text-muted text-uppercase">Nama Layanan Jasa</label>
                                <div class="input-group shadow-sm rounded-3 overflow-hidden border">
                                    <span class="input-group-text bg-white border-0"><i class="fas fa-screwdriver-wrench text-success"></i></span>
                                    <input type="text" name="name" class="form-control border-0 py-2" value="<%= service.name %>" placeholder="Contoh: Service Ringan" required>
                                </div>
                            </div>
                            <div class="col-md-4">
                                <label class="form-label small fw-bold text-muted text-uppercase">Kode Jasa</label>
                                <div class="input-group shadow-sm rounded-3 overflow-hidden border">
                                    <span class="input-group-text bg-white border-0"><i class="fas fa-hashtag text-success"></i></span>
                                    <input type="text" name="code" class="form-control border-0 py-2 font-monospace" value="<%= service.code %>" placeholder="JSA-01" required>
                                </div>
                            </div>
                        </div>

                        <div class="mb-4">
                            <label class="form-label small fw-bold text-muted text-uppercase">Biaya Jasa (IDR)</label>
                            <div class="input-group shadow-sm rounded-3 overflow-hidden border">
                                <span class="input-group-text bg-success bg-opacity-10 border-0 text-success fw-bold">Rp</span>
                                <input type="number" name="price" class="form-control border-0 py-2" value="<%= service.price %>" placeholder="0" required>
                            </div>
                            <div class="form-text small text-muted">Tentukan biaya jasa tetap (flat rate).</div>
                        </div>

                        <div class="mb-5">
                            <label class="form-label small fw-bold text-muted text-uppercase">Deskripsi Pengerjaan</label>
                            <div class="input-group shadow-sm rounded-3 overflow-hidden border">
                                <span class="input-group-text bg-white border-0 align-items-start pt-2"><i class="fas fa-align-left text-muted"></i></span>
                                <textarea name="description" class="form-control border-0 py-2" rows="4" placeholder="Jelaskan detail apa saja yang dikerjakan..."><%= service.description %></textarea>
                            </div>
                        </div>

                        <hr class="my-4 opacity-10">

                        <div class="row g-3">
                            <div class="col-md-8">
                                <button type="submit" class="btn btn-success btn-emerald w-100 py-3 fw-bold rounded-3 shadow">
                                    <i class="fas fa-save me-2"></i>SIMPAN PERUBAHAN
                                </button>
                            </div>
                            <div class="col-md-4">
                                <a href="/services" class="btn btn-outline-secondary w-100 py-3 fw-bold rounded-3">
                                    BATAL
                                </a>
                            </div>
                        </div>
                    </form>
                </div>
            </div>
            
            <div class="text-center mt-4">
                <small class="text-muted italic">
                    <i class="fas fa-info-circle me-1"></i> Perubahan pada master jasa akan mempengaruhi entri transaksi baru di masa depan.
                </small>
            </div>
        </div>
    </div>
</div>

<style>
    .bg-emerald-gradient {
        background: linear-gradient(135deg, #2ecc71 0%, #27ae60 100%);
    }

    .font-monospace {
        font-family: 'Courier New', Courier, monospace;
        letter-spacing: 1px;
    }

    .form-control:focus {
        box-shadow: none;
        background-color: #fff !important;
        border: 1px solid #2ecc71 !important;
    }

    .btn-emerald {
        background-color: #2ecc71;
        border: none;
        transition: all 0.3s ease;
    }

    .btn-emerald:hover {
        background-color: #27ae60;
        transform: translateY(-2px);
        box-shadow: 0 8px 20px rgba(46, 204, 113, 0.3) !important;
    }

    .animate-fade-in {
        animation: fadeIn 0.8s ease-in-out;
    }

    @keyframes fadeIn {
        from { opacity: 0; transform: translateY(20px); }
        to { opacity: 1; transform: translateY(0); }
    }
</style>