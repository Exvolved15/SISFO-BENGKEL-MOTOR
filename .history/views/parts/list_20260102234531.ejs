<div class="container-fluid px-4 py-4 animate-fade-in">
    <div class="d-flex flex-column flex-md-row justify-content-between align-items-md-center mb-4 gap-3">
        <div>
            <h1 class="h3 fw-bold text-dark mb-1"><%= title %></h1>
            <p class="text-muted mb-0">Kelola ketersediaan suku cadang bengkel.</p>
        </div>
        <a href="/parts/add" class="btn btn-success px-4 py-2 fw-bold rounded-3 shadow-sm btn-emerald">
            <i class="fas fa-plus-circle me-2"></i>Tambah Suku Cadang
        </a>
    </div>

    <div class="row g-3 mb-4">
        <div class="col-md-4">
            <div class="card border-0 shadow-sm rounded-4 p-3 bg-white border-start border-4 border-primary">
                <h6 class="text-muted small mb-1">Total Jenis Barang</h6>
                <h4 class="fw-bold mb-0"><%= parts.length %></h4>
            </div>
        </div>
        <div class="col-md-4">
            <% const lowStock = parts.filter(p => p.stock <= 5).length; %>
            <div class="card border-0 shadow-sm rounded-4 p-3 bg-white border-start border-4 border-danger">
                <h6 class="text-muted small mb-1">Stok Menipis</h6>
                <h4 class="fw-bold mb-0 text-danger"><%= lowStock %> Item</h4>
            </div>
        </div>
        <div class="col-md-4">
            <div class="card border-0 shadow-sm rounded-4 p-3 bg-white border-start border-4 border-success">
                <h6 class="text-muted small mb-1">Nilai Inventaris</h6>
                <h4 class="fw-bold mb-0">Rp <%= parts.reduce((acc, p) => acc + (p.sellingPrice * p.stock), 0).toLocaleString('id-ID') %></h4>
            </div>
        </div>
    </div>

    <div class="card border-0 shadow-sm rounded-4 overflow-hidden">
        <div class="card-header bg-white py-3 border-bottom border-light">
            <input type="text" id="partSearch" class="form-control form-control-sm rounded-pill px-3 border-secondary" placeholder="Cari barang...">
        </div>
        <div class="card-body p-0">
            <div class="table-responsive">
                <table class="table table-hover align-middle mb-0" id="partTable">
                    <thead class="table-light">
                        <tr class="small text-uppercase fw-bold text-muted">
                            <th class="ps-4">Preview</th>
                            <th>Suku Cadang</th>
                            <th class="text-center">Stok</th>
                            <th>Harga Jual</th>
                            <th class="text-center pe-4">Aksi</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% parts.forEach(part => { %>
                            <tr>
                                <td class="ps-4">
                                    <button class="btn btn-sm btn-outline-success rounded-pill px-3 shadow-sm" type="button" data-bs-toggle="collapse" data-bs-target="#img-<%= part._id %>">
                                        <i class="fas fa-image me-1"></i> Preview
                                    </button>
                                    
                                    <div class="collapse" id="img-<%= part._id %>" style="position: fixed; z-index: 9999; top: 50%; left: 50%; transform: translate(-50%, -50%); width: 280px;">
                                        <div class="bg-white p-3 rounded-4 shadow-lg border-start border-4 border-success text-center">
                                            <h6 class="small fw-bold text-dark mb-2"><%= part.name %></h6>
                                            <img src="<%= part.imageUrl %>" 
                                                style="width: 220px; height: 220px; object-fit: cover; border-radius: 12px;"
                                                onerror="this.src='https://ui-avatars.com/api/?name=<%= part.name.replace(/\s/g, '+') %>&background=2ecc71&color=fff&size=200';">
                                            <button class="btn btn-sm btn-secondary w-100 mt-3 rounded-pill" type="button" data-bs-toggle="collapse" data-bs-target="#img-<%= part._id %>">Tutup</button>
                                        </div>
                                    </div>
                                </td>
                                <td>
                                    <div class="fw-bold text-dark"><%= part.name %></div>
                                    <span class="badge bg-light text-dark border font-monospace" style="font-size: 0.7rem;"><%= part.code %></span>
                                </td>
                                <td class="text-center">
                                    <span class="badge <%= part.stock <= 5 ? 'bg-danger pulse-danger' : 'bg-success' %> rounded-pill px-3"><%= part.stock %></span>
                                </td>
                                <td class="fw-bold text-success">Rp <%= part.sellingPrice.toLocaleString('id-ID') %></td>
                                <td class="text-center pe-4">
                                    <div class="btn-group">
                                        <a href="/api/parts/<%= part._id %>/edit" class="btn btn-sm btn-white border"><i class="fas fa-edit text-warning"></i></a>
                                        <form action="/api/parts/delete/<%= part._id %>?_method=DELETE" method="POST" class="d-inline">
                                            <button type="submit" class="btn btn-sm btn-white border" onclick="return confirm('Hapus?')"><i class="fas fa-trash-alt text-danger"></i></button>
                                        </form>
                                    </div>
                                </td>
                            </tr>
                        <% }); %>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

<style>
    .font-monospace { font-family: 'Courier New', Courier, monospace; }
    .btn-emerald:hover { background-color: #27ae60; transform: translateY(-2px); }
    .pulse-danger { animation: pulse-red 2s infinite; }
    @keyframes pulse-red {
        0% { box-shadow: 0 0 0 0 rgba(220, 53, 69, 0.7); }
        70% { box-shadow: 0 0 0 10px rgba(220, 53, 69, 0); }
        100% { box-shadow: 0 0 0 0 rgba(220, 53, 69, 0); }
    }
</style>