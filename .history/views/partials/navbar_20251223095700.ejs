<nav class="navbar navbar-expand-lg navbar-dark bg-dark shadow-sm py-2">
    <div class="container">
        <a class="navbar-brand fw-bold text-warning" href="/">
            <i class="fas fa-wrench me-2"></i>SISFO BENGKEL
        </a>

        <div class="d-flex align-items-center ms-lg-3 me-auto">
            <ul class="nav flex-row align-items-center">
                <li class="nav-item">
                    <a class="nav-link px-2 text-white <%= (typeof activePage !== 'undefined' && activePage === 'home') ? 'fw-bold border-bottom border-warning' : '' %>" href="/">Overview</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link px-2 text-white <%= (typeof activePage !== 'undefined' && activePage === 'about') ? 'fw-bold border-bottom border-warning' : '' %>" href="/about">About</a>
                </li>
            </ul>
        </div>

        <% if (typeof user !== 'undefined' && user) { %>
            <button class="navbar-toggler border-0 shadow-none" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
        <% } %>

        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto align-items-lg-center">
                
                <% if (typeof user !== 'undefined' && user) { %>
                    <hr class="d-lg-none text-white-50"> <% if (user.role === 'admin' || user.role === 'kasir') { %>
                        <li class="nav-item">
                            <a class="nav-link <%= (typeof activePage !== 'undefined' && activePage === 'parts') ? 'active' : '' %>" href="/parts">Suku Cadang</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link <%= (typeof activePage !== 'undefined' && activePage === 'services') ? 'active' : '' %>" href="/services">Jasa Servis</a>
                        </li>
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" href="#" id="transaksiDrop" role="button" data-bs-toggle="dropdown">
                                Transaksi
                            </a>
                            <ul class="dropdown-menu dropdown-menu-dark shadow border-secondary">
                                <li><a class="dropdown-item" href="/transactions/add">Buat Transaksi Baru</a></li>
                                <li><a class="dropdown-item" href="/transactions">Riwayat Transaksi</a></li>
                            </ul>
                        </li>
                    <% } %>

                    <% if (user.role === 'mekanik') { %>
                        <li class="nav-item">
                            <a class="nav-link <%= (typeof activePage !== 'undefined' && activePage === 'dashboard') ? 'active text-warning' : '' %>" href="/api/jobs/mechanic/dashboard">
                                <i class="fas fa-tools me-1"></i> Pekerjaan Saya
                            </a>
                        </li>
                    <% } %>

                    <hr class="d-lg-none text-white-50">

                    <li class="nav-item dropdown ms-lg-3">
                        <a class="nav-link dropdown-toggle d-flex align-items-center bg-secondary bg-opacity-25 rounded-pill px-3 py-1" href="#" id="userProfileDrop" role="button" data-bs-toggle="dropdown">
                            <div class="bg-warning text-dark rounded-circle d-flex align-items-center justify-content-center me-2" style="width: 24px; height: 24px; font-size: 0.75rem; font-weight: bold;">
                                <%= user.name.charAt(0).toUpperCase() %>
                            </div>
                            <span class="small text-white"><%= user.name %></span>
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end dropdown-menu-dark shadow border-secondary mt-2">
                            <li class="px-3 py-2"><small class="text-muted d-block text-uppercase" style="font-size: 0.65rem;">Role Anda</small><span class="badge bg-info text-dark"><%= user.role.toUpperCase() %></span></li>
                            <li><hr class="dropdown-divider"></li>
                            <li><a class="dropdown-item" href="/users/edit/<%= user._id %>"><i class="fas fa-user-circle me-2"></i>Edit Profil</a></li>
                            <li><a class="dropdown-item text-danger" id="logout-link" href="/logout"><i class="fas fa-sign-out-alt me-2"></i>Logout</a></li>
                        </ul>
                    </li>

                    <script type="module">
                        import { getAuth, signOut } from "https://www.gstatic.com/firebasejs/10.12.2/firebase-auth.js";
                        const logoutLink = document.getElementById('logout-link');
                        if (logoutLink) {
                            logoutLink.addEventListener('click', async (e) => {
                                e.preventDefault();
                                try {
                                    const auth = getAuth();
                                    await signOut(auth);
                                } catch (error) {
                                    console.error("Firebase Signout Error:", error);
                                }
                                window.location.href = '/logout';
                            });
                        }
                    </script>

                <% } else { %>
                    <li class="nav-item ms-lg-2">
                        <a href="/login" class="btn btn-primary btn-sm px-4 btn-hover shadow-sm">
                            <i class="fas fa-sign-in-alt me-1"></i> Masuk
                        </a>
                    </li>
                <% } %>
            </ul>
        </div>
    </div>
</nav>

<style>
    .btn-hover { transition: transform 0.2s ease; }
    .btn-hover:hover { transform: translateY(-2px); }
    .dropdown-item:hover { background-color: rgba(255, 193, 7, 0.2); color: #ffc107; }
    .nav-link.active { border-bottom: 2px solid #ffc107; color: #ffc107 !important; }
</style>