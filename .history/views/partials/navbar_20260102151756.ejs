<nav class="navbar navbar-expand-lg navbar-dark bg-dark shadow-sm">
    <div class="container">
        <a class="navbar-brand fw-bold d-flex align-items-center" href="<%= (typeof user !== 'undefined' && user) ? '/dashboard-redirect' : '/' %>">
            <div class="bg-success text-white rounded-3 p-1 me-2 d-flex align-items-center justify-content-center" style="width: 32px; height: 32px;">
                <i class="fas fa-screwdriver-wrench"></i>
            </div>
            <span>ACR Motor</span>
        </a>

        <div class="d-flex align-items-center">
            <ul class="nav flex-row align-items-center d-none d-lg-flex">
                <li class="nav-item">
                    <a class="nav-link px-2 text-white <%= (locals.activePage === 'home') ? 'fw-bold opacity-100' : 'opacity-75' %>" href="/">Overview</a>
                </li>
                <li class="nav-item border-start border-secondary ms-2 ps-2">
                    <a class="nav-link px-2 text-white <%= (locals.activePage === 'about') ? 'fw-bold opacity-100' : 'opacity-75' %>" href="/about">About</a>
                </li>
            </ul>
        </div>

        <button class="navbar-toggler border-0 shadow-none ms-auto" type="button" data-bs-toggle="collapse" data-bs-target="#navInternal">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navInternal">
            <ul class="navbar-nav ms-auto align-items-lg-center">
                <% if (typeof user !== 'undefined' && user) { %>
                    <% if (user.role === 'admin' || user.role === 'kasir') { %>
                        <li class="nav-item"><a class="nav-link small" href="/parts"><i class="fas fa-box me-1"></i> Suku Cadang</a></li>
                        <li class="nav-item"><a class="nav-link small" href="/services"><i class="fas fa-wrench me-1"></i> Jasa</a></li>
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle small" href="#" data-bs-toggle="dropdown">Transaksi</a>
                            <ul class="dropdown-menu dropdown-menu-dark shadow">
                                <li><a class="dropdown-item small" href="/transactions/add">Baru</a></li>
                                <li><a class="dropdown-item small" href="/transactions">Riwayat</a></li>
                            </ul>
                        </li>
                    <% } %>

                    <% if (user.role === 'mekanik') { %>
                        <li class="nav-item"><a class="nav-link small" href="/api/jobs/mechanic/dashboard"><i class="fas fa-tasks me-1"></i>Tugas</a></li>
                    <% } %>

                    <li class="nav-item dropdown ms-lg-3 mt-2 mt-lg-0">
                        <a class="nav-link dropdown-toggle d-flex align-items-center bg-white bg-opacity-10 px-3 py-1 rounded-pill" href="#" data-bs-toggle="dropdown">
                            <% if (user.profileImage && user.profileImage !== '/img/default-avatar.png') { %>
                                <img src="<%= user.profileImage %>" class="rounded-circle me-2 border border-success" style="width: 28px; height: 28px; object-fit: cover;">
                            <% } else { %>
                                <div class="bg-success text-white rounded-circle d-flex align-items-center justify-content-center me-2" style="width: 28px; height: 28px; font-size: 0.7rem;"><%= user.name.charAt(0).toUpperCase() %></div>
                            <% } %>
                            <span class="small fw-bold text-white"><%= user.name.split(' ')[0] %></span>
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end dropdown-menu-dark rounded-3">
                            <li><a class="dropdown-item small" href="/users/edit/<%= user._id %>"><i class="fas fa-user-edit me-2"></i>Edit Profil</a></li>
                            <li><hr class="dropdown-divider border-secondary"></li>
                            <li><a class="dropdown-item small text-danger fw-bold" href="/logout"><i class="fas fa-sign-out-alt me-2"></i>Keluar</a></li>
                        </ul>
                    </li>
                <% } else { %>
                    <li class="nav-item ms-lg-3"><a href="/login" class="btn btn-success btn-sm px-4 rounded-pill fw-bold shadow-sm">Masuk</a></li>
                <% } %>
            </ul>
        </div>
    </div>
</nav>

<style>
    /* 1. Efek Highlight Garis Bawah */
    .active-highlight::after {
        content: "";
        position: absolute;
        bottom: -2px;
        left: 8px;
        right: 8px;
        height: 3px;
        background: #2ecc71;
        border-radius: 10px;
        animation: expand 0.3s ease-out;
    }

    @keyframes expand {
        from { width: 0; left: 50%; }
        to { width: auto; left: 8px; }
    }

    /* 2. Animasi Dropdown Slide-In */
    .animate-slide-in {
        animation: slideIn 0.2s ease-out;
    }

    @keyframes slideIn {
        from { opacity: 0; transform: translateY(10px); }
        to { opacity: 1; transform: translateY(0); }
    }

    /* 3. Perbaikan Visual Profile Pill */
    .profile-pill {
        background: rgba(255, 255, 255, 0.05);
        transition: all 0.3s ease;
        border: 1px solid rgba(255, 255, 255, 0.1);
    }

    .profile-pill:hover {
        background: rgba(46, 204, 113, 0.15);
        border-color: rgba(46, 204, 113, 0.4);
    }

    .hover-link {
        transition: color 0.3s ease;
    }

    .hover-link:hover {
        color: #2ecc71 !important;
    }

    .btn-hover-grow {
        transition: transform 0.2s ease;
    }

    .btn-hover-grow:hover {
        transform: scale(1.05);
    }
    /* Memperbaiki area profil di navbar agar tidak tumpang tindih atau terpotong */
    .nav-profile-dropdown {
        display: flex;
        align-items: center;
        background: rgba(255, 255, 255, 0.1);
        padding: 5px 15px;
        border-radius: 50px;
        transition: all 0.3s ease;
        border: 1px solid rgba(255, 255, 255, 0.2);
    }

    .nav-profile-dropdown:hover {
        background: rgba(255, 255, 255, 0.2);
    }

    /* Memastikan dropdown menu muncul di atas elemen lain */
    .dropdown-menu-end {
        right: 0;
        left: auto;
        z-index: 9999 !important;
        margin-top: 10px;
        box-shadow: 0 10px 30px rgba(0,0,0,0.1);
        border: none;
        border-radius: 15px;
    }

    /* Mengatur ukuran foto profil di navbar agar konsisten */
    .navbar-avatar {
        width: 35px;
        height: 35px;
        object-fit: cover;
        border: 2px solid #fff;
    }
</style>