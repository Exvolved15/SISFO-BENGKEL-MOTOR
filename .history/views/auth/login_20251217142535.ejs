<script type="module">
    // =========================================================
    // === 1. IMPOR & INISIASI APP (SOLUSI FINAL UNTUK CLIENT) ===
    // =========================================================
    
    // Impor fungsi yang dibutuhkan dari URL CDN
    import { initializeApp } from "https://www.gstatic.com/firebasejs/10.12.2/firebase-app.js"; 
    import { getAuth, signInWithEmailAndPassword, signOut } from "https://www.gstatic.com/firebasejs/10.12.2/firebase-auth.js"; 

    // Konfigurasi Firebase Client Anda (Data ini sudah OK)
    const firebaseConfig = {
        apiKey: "AIzaSyBNclQBPgOf9wS0hCM6jsWerAJ_-mz_AOA",
        authDomain: "bengkelan-motor.firebaseapp.com",
        projectId: "bengkelan-motor",
        storageBucket: "bengkelan-motor.firebasestorage.app",
        messagingSenderId: "86184705378",
        appId: "1:86184705378:web:018c12ce6261c9a522c03b",
        measurementId: "G-5WMTCL5G9R"
    };

    // --- INISIASI APP DAN AUTH SECARA KONDISIONAL DAN AMAN ---
    let app;
    try {
        // Cek apakah app sudah ada di browser, jika belum, inisiasi
        app = initializeApp(firebaseConfig);
    } catch (e) {
        // Jika app sudah ada (misalnya di file lain), kita abaikan inisiasi
        console.warn("Firebase App sudah diinisiasi di tempat lain. Menggunakan instance yang sudah ada.");
    }
    
    // Ambil instance Auth yang sudah terinisiasi (gunakan app sebagai argumen)
    const auth = getAuth(app); 
    // ------------------------------------------------------------------------


    // =========================================================
    // === 2. EVENT LISTENER UNTUK LOGIN (LOGIKA LOGIN & LOGOUT) ===
    // =========================================================

    document.getElementById('loginForm').addEventListener('submit', async (e) => {
        e.preventDefault(); 
        
        const email = document.getElementById('email').value;
        const password = document.getElementById('password').value;
        const errorMessageDiv = document.getElementById('errorMessage');
        
        errorMessageDiv.textContent = '';
        errorMessageDiv.classList.add('d-none');
        
        // --- KRITIS: SIGN OUT USER SEBELUMNYA SECARA TEGAS ---
        try {
            await signOut(auth); // Ini akan membersihkan token Admin lama
            console.log("CLIENT: User sebelumnya berhasil di-sign out dari Firebase state.");
        } catch (error) {
            // Abaikan error
        }
        // ----------------------------------------------------

        try {
            // 3. Login Sisi Klien di Firebase
            const userCredential = await signInWithEmailAndPassword(auth, email, password);
            const idToken = await userCredential.user.getIdToken(); 
            
            console.log("CLIENT: Login Firebase sukses, ID Token baru didapat.");

            // 4. Kirim Token ke Server Kita (Verifikasi Sesi)
            const res = await fetch('/api/auth/verify-token', {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({ idToken: token })
            });

            if (!response.ok) {
                const errorData = await response.json();
                throw new Error(errorData.message || 'Verifikasi server gagal.');
            }

            // 5. Sukses: Redirect ke dashboard
            window.location.href = '/'; 

        } catch (error) {
            // ... Tangani Error dan Tampilkan Pesan ...
            console.error("Login Gagal:", error); 
            
            let message = 'Login Gagal: Silakan periksa kembali kredensial Anda.';
            
            if (error.code && (error.code.includes('auth/invalid-credential') || error.code.includes('auth/user-not-found') || error.code.includes('auth/wrong-password'))) {
                 message = 'Email atau kata sandi salah.';
            } else if (error.message) {
                 message = error.message;
            }
            
            errorMessageDiv.textContent = message;
            errorMessageDiv.classList.remove('d-none');
        }
    });
</script>

<body class="bg-light"> 
    
    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-6 col-lg-5"> 
                <div class="card shadow-lg p-4"> 
                    <h2 class="text-center mb-4">Login Sisfo Bengkel</h2>
                    
                    <form id="loginForm"> 
                        <% if (query.success) { %>
                            <div class="alert alert-success text-center">Registrasi berhasil! Silakan login.</div>
                        <% } %>
                        
                        <div id="errorMessage" class="alert alert-danger d-none" role="alert"></div>

                        <div class="mb-3">
                            <label for="email" class="form-label">Email</label>
                            <input type="email" class="form-control" id="email" name="email" required>
                        </div>
                        <div class="mb-3">
                            <label for="password" class="form-label">Password</label>
                            <input type="password" 
                                   class="form-control" 
                                   id="password" 
                                   name="password" 
                                   required
                                   autocomplete="current-password"> </div>
                        </div>
                        
                        <button type="submit" class="btn btn-primary w-100 mb-3">Login</button>
                    </form>

                    <div class="text-center mt-3">
                        Belum punya akun? <a href="/register">Daftar</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>