<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/bootstrap.min.css"> 
    <link rel="stylesheet" href="/css/style.css"> 
</head>
<body class="bg-light"> 
    
    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-6 col-lg-5"> 
                <div class="card shadow-lg p-4"> 
                    <h2 class="text-center mb-4">
                        <% if (title === 'Login') { %> Login Sisfo Bengkel <% } %>
                        <% if (title === 'Register User') { %> Register User Baru <% } %>
                    </h2>
                    
                    <form id="<%= (title === 'Login') ? 'loginForm' : 'registerForm' %>"> 
                        </form>

                    <div class="text-center mt-3">
                        <% if (title === 'Login') { %> Belum punya akun? <a href="/register">Daftar</a> <% } %>
                        <% if (title === 'Register User') { %> Sudah punya akun? <a href="/login">Login</a> <% } %>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="/js/bootstrap.bundle.min.js"></script>
    <script type="module">
        // ... Logika Firebase Client ...
    </script>
</body>
</html>