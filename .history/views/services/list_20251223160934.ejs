<div class="container-fluid px-4 py-4 animate-fade-in">
    <div class="d-flex flex-column flex-md-row justify-content-between align-items-md-center mb-4 gap-3">
        <div>
            <h1 class="h3 fw-bold text-dark mb-1"><%= title %></h1>
            <p class="text-muted mb-0">Kelola katalog layanan jasa servis dan biaya perbaikan bengkel.</p>
        </div>
        <div>
            <a href="/services/add" class="btn btn-success px-4 py-2 fw-bold rounded-3 shadow-sm btn-emerald">
                <i class="fas fa-plus-circle me-2"></i>Tambah Jasa Baru
            </a>
        </div>
    </div>

    <div class="card border-0 shadow-sm rounded-4 overflow-hidden">
        <div class="card-header bg-white py-3 border-bottom border-light">
            <div class="row align-items-center">
                <div class="col">
                    <h6 class="mb-0 fw-bold">
                        <i class="fas fa-hand-holding-heart me-2 text-success"></i>Katalog Layanan Servis
                    </h6>
                </div>
                <div class="col-auto">
                    <div class="input-group input-group-sm shadow-sm rounded-pill overflow-hidden border">
                        <span class="input-group-text bg-white border-0"><i class="fas fa-search text-muted"></i></span>
                        <input type="text" id="serviceSearch" class="form-control border-0 ps-0" placeholder="Cari nama jasa...">
                    </div>
                </div>
            </div>
        </div>
        <div class="card-body p-0">
            <% if (services && services.length > 0) { %>
                <div class="table-responsive">
                    <table class="table table-hover align-middle mb-0" id="serviceTable">
                        <thead class="table-light">
                            <tr class="small text-uppercase fw-bold text-muted">
                                <th class="ps-4">No.</th>
                                <th>Nama Layanan Jasa</th>
                                <th>Deskripsi Singkat</th>
                                <th>Biaya (Estimasi)</th>
                                <th class="text-center pe-4">Aksi</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% services.forEach((svc, index) => { %>
                                <tr>
                                    <td class="ps-4 text-muted"><%= index + 1 %></td>
                                    <td>
                                        <div class="fw-bold text-dark"><%= svc.name %></div>
                                        <small class="text-muted small">ID: <%= svc._id.toString().slice(-6).toUpperCase() %></small>
                                    </td>
                                    <td class="text-muted small" style="max-width: 300px;">
                                        <%= svc.description || 'Pengerjaan standar teknisi.' %>
                                    </td>
                                    <td>
                                        <span class="badge bg-success bg-opacity-10 text-success px-3 py-2 rounded-pill fw-bold border border-success border-opacity-25">
                                            Rp <%= (svc.price || 0).toLocaleString('id-ID') %>
                                        </span>
                                    </td>
                                    <td class="text-center pe-4">
                                        <div class="btn-group shadow-sm rounded-3">
                                            <a href="/api/services/edit/<%= svc._id %>" class="btn btn-sm btn-white border border-light" title="Edit Jasa">
                                                <i class="fas fa-edit text-warning"></i>
                                            </a>
                                            <form action="/api/services/delete/<%= svc._id %>?_method=DELETE" method="POST" class="d-inline">
                                                <button type="submit" class="btn btn-sm btn-white border border-light" 
                                                    onclick="return confirm('Hapus layanan jasa ini?');">
                                                    <i class="fas fa-trash-alt text-danger"></i>
                                                </button>
                                            </form>
                                        </div>
                                    </td>
                                </tr>
                            <% }); %>
                        </tbody>
                    </table>
                </div>
            <% } else { %>
                <div class="text-center py-5">
                    <i class="fas fa-concierge-bell fa-4x text-muted opacity-25 mb-3"></i>
                    <h5 class="text-muted">Katalog Jasa Belum Tersedia</h5>
                    <p class="small text-muted">Mulai tambahkan jenis layanan jasa servis untuk memproses transaksi.</p>
                </div>
            <% } %>
        </div>
    </div>
</div>

<style>
    /* Sinkronisasi Tema Emerald */
    .btn-white { background: #fff; }
    .btn-white:hover { background: #f8f9fa; }
    
    .btn-emerald { background-color: #2ecc71; border: none; transition: 0.3s; color: white; }
    .btn-emerald:hover { background-color: #27ae60; transform: translateY(-2px); box-shadow: 0 5px 15px rgba(46, 204, 113, 0.3); color: white; }

    .animate-fade-in { animation: fadeIn 0.5s ease-out; }
    @keyframes fadeIn { from { opacity: 0; transform: translateY(10px); } to { opacity: 1; transform: translateY(0); } }
</style>

<script>
    // Fitur Pencarian Jasa Real-time
    document.getElementById('serviceSearch').addEventListener('keyup', function() {
        let value = this.value.toLowerCase();
        let rows = document.querySelectorAll("#serviceTable tbody tr");
        
        rows.forEach(row => {
            let text = row.innerText.toLowerCase();
            row.style.display = text.includes(value) ? "" : "none";
        });
    });
</script>