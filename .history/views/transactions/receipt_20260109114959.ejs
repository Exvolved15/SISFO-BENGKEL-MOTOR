<!DOCTYPE html>
<html>
<head>
    <title>Struk - <%= transaction.invoiceNumber %></title>
    <style>
        body { font-family: 'Courier New', Courier, monospace; width: 300px; margin: auto; padding: 20px; color: #000; }
        .text-center { text-align: center; }
        .header { border-bottom: 1px dashed #000; padding-bottom: 10px; margin-bottom: 10px; }
        .item-table { width: 100%; font-size: 12px; }
        .footer { border-top: 1px dashed #000; margin-top: 10px; padding-top: 10px; font-size: 10px; }
        @media print { .no-print { display: none; } }
    </style>
</head>
<body onload="window.print()">
    <div class="no-print" style="margin-bottom: 20px;">
        <button onclick="window.print()">Cetak Sekarang</button>
        <button onclick="window.close()">Tutup</button>
    </div>

    <div class="header text-center">
        <h3 style="margin:0;">ACR MOTOR</h3>
        <p style="font-size: 10px; margin:0;">Jl. Abdullah Daeng Sirua No.170a, Makassar</p>
        <p style="font-size: 10px;">WA: 081234567890</p>
    </div>

    <div style="font-size: 12px; margin-bottom: 10px;">
        No: <%= transaction.invoiceNumber %><br>
        Tgl: <%= new Date(transaction.createdAt).toLocaleString('id-ID') %><br>
        Motor: <%= transaction.motorName %> (<%= transaction.vehicleLicense %>)
    </div>

    <table class="item-table">
        <tr style="border-bottom: 1px solid #000;">
            <th align="left">Layanan/Part</th>
            <th align="right">Total</th>
        </tr>
        <tr>
            <td><%= transaction.description %></td>
            <td align="right"><%= transaction.totalPrice.toLocaleString('id-ID') %></td>
        </tr>
    </table>

    <div class="footer text-center">
        <p>Total: Rp <%= transaction.totalPrice.toLocaleString('id-ID') %></p>
        <p>Terima Kasih Atas Kepercayaan Anda<br>Simpan struk ini sebagai bukti garansi.</p>
    </div>
</body>
</html>