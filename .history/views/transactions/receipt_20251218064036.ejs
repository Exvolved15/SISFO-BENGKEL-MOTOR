<!DOCTYPE html>
<html>
<head>
    <title><%= title %></title>
    <style>
        body { font-family: 'Courier New', monospace; padding: 20px; width: 80mm; margin: auto; }
        .header { text-align: center; border-bottom: 1px dashed #000; padding-bottom: 10px; }
        .content { margin-top: 10px; font-size: 12px; }
        .footer { text-align: center; margin-top: 20px; border-top: 1px dashed #000; padding-top: 10px; }
        table { width: 100%; font-size: 12px; }
        .btn-print { background: #333; color: #fff; padding: 5px 10px; text-decoration: none; border-radius: 4px; }
        @media print { .no-print { display: none; } }
    </style>
</head>
<body>
    <div class="no-print" style="margin-bottom: 20px;">
        <button onclick="window.print()" class="btn-print">Klik Cetak / Save PDF</button>
        <a href="/transactions" style="margin-left: 10px;">Kembali</a>
    </div>

    <div class="header">
        <h3>BENGKEL JAYA</h3>
        <p>Jl. Contoh No. 123<br>Invoice: <%= transaction.invoiceNumber %></p>
    </div>

    <div class="content">
        <p>Tgl: <%= new Date(transaction.createdAt).toLocaleString('id-ID') %><br>
        Plgn: <%= transaction.customerName %><br>
        Kend: <%= transaction.vehicleLicense %></p>
        
        <hr>
        <table>
            <% transaction.itemDetails.forEach(item => { %>
            <tr>
                <td><%= item.name %> x<%= item.quantity %></td>
                <td style="text-align: right;">Rp <%= (item.pricePerUnit * item.quantity).toLocaleString('id-ID') %></td>
            </tr>
            <% }) %>
        </table>
        <hr>
        <p style="text-align: right;"><strong>Total: Rp <%= transaction.totalAmount.toLocaleString('id-ID') %></strong></p>
        <p>Metode: <%= transaction.paymentMethod %></p>
    </div>

    <div class="footer">
        <p>Terima Kasih Atas Kepercayaan Anda</p>
    </div>

    <script>
        // Otomatis buka jendela print saat halaman dimuat
        window.onload = function() { window.print(); }
    </script>
</body>
</html>