<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Nota_<%= transaction.invoiceNumber %></title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { 
            font-family: 'Courier New', Courier, monospace; 
            width: 80mm; 
            margin: 0 auto; 
            padding: 10px;
            font-size: 12px; 
            color: #000; 
        }
        .text-center { text-align: center; }
        .text-right { text-align: right; }
        .fw-bold { font-weight: bold; }
        .uppercase { text-transform: uppercase; }
        .brand-name { font-size: 18px; font-weight: bold; margin-bottom: 2px; }
        .brand-sub { font-size: 10px; margin-bottom: 5px; line-height: 1.2; }
        .divider { border-bottom: 1px dashed #000; margin: 5px 0; }
        .divider-double { border-bottom: 2px double #000; margin: 5px 0; }
        .info-table, .item-table, .total-table { width: 100%; border-collapse: collapse; font-size: 11px; }
        .item-row-name { padding-top: 5px; }
        .grand-total { font-size: 14px; border-top: 1px solid #000; padding-top: 5px; }
        @media print { .no-print { display: none !important; } }
        .btn-group { margin-top: 20px; display: flex; gap: 5px; justify-content: center; }
        .btn { padding: 5px 10px; border: 1px solid #000; cursor: pointer; }
    </style>
</head>
<body onload="window.print()">

    <div class="text-center">
        <h1 class="brand-name">ACR MOTOR</h1>
        <div class="brand-sub">
            Jl. Abdullah Daeng Sirua No.170a, Makassar<br>
            Telp: 0812-3456-7890
        </div>
        <div class="divider-double"></div>
    </div>

    <table class="info-table">
        <tr><td>No. Invoice</td><td>:</td><td class="fw-bold"><%= transaction.invoiceNumber %></td></tr>
        <tr><td>Tanggal</td><td>:</td><td><%= new Date(transaction.createdAt).toLocaleString('id-ID') %></td></tr>
        <tr><td>Unit/Nopol</td><td>:</td><td><%= transaction.motorName %> / <%= transaction.vehicleLicense %></td></tr>
        <tr><td>Mekanik</td><td>:</td><td><%= transaction.mechanicName || '-' %></td></tr>
    </table>

    <div class="divider"></div>

    <table class="item-table">
        <% if (locals.details && details.length > 0) { %>
            <% details.forEach(item => { %>
                <tr>
                    <td colspan="2" class="item-row-name uppercase fw-bold">
                        [<%= item.itemType === 'part' ? 'P' : 'J' %>] <%= item.itemName %>
                    </td>
                </tr>
                <tr>
                    <td>&nbsp;&nbsp;<%= item.quantity %> x <%= item.pricePerUnit.toLocaleString('id-ID') %></td>
                    <td class="text-right"><%= (item.quantity * item.pricePerUnit).toLocaleString('id-ID') %></td>
                </tr>
            <% }); %>
        <% } else { %>
            <tr><td colspan="2" class="text-center">-- Jasa Servis --</td></tr>
            <tr>
                <td>Pengerjaan</td>
                <td class="text-right"><%= (transaction.totalAmount || 0).toLocaleString('id-ID') %></td>
            </tr>
        <% } %>
    </table>

    <div class="divider"></div>

    <table class="total-table">
        <tr>
            <td>Subtotal</td>
            <td class="text-right">Rp <%= (transaction.totalAmount || 0).toLocaleString('id-ID') %></td>
        </tr>
        <% if (transaction.discount > 0) { %>
        <tr>
            <td>Potongan</td>
            <td class="text-right">-Rp <%= transaction.discount.toLocaleString('id-ID') %></td>
        </tr>
        <% } %>
        <tr class="grand-total fw-bold">
            <td>TOTAL AKHIR</td>
            <td class="text-right">Rp <%= (transaction.grandTotal || transaction.totalAmount || 0).toLocaleString('id-ID') %></td>
        </tr>
        <tr>
            <td>Bayar</td>
            <td class="text-right uppercase"><%= transaction.paymentMethod %></td>
        </tr>
    </table>

    <div class="divider"></div>

    <div class="text-center" style="font-size: 10px; margin-top: 10px;">
        <p class="fw-bold">TERIMA KASIH</p>
        <p>Simpan nota ini sebagai bukti garansi.</p>
        <div style="margin-top: 5px; border: 1px solid #000; padding: 2px;">SISFO BENGKEL DIGITAL</div>
    </div>

    <div class="btn-group no-print">
        <button onclick="window.print()" class="btn" style="background:#000; color:#fff;">Cetak</button>
        <button onclick="window.close()" class="btn">Tutup</button>
    </div>

</body>
</html>