<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> | ACR Motor</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://unpkg.com/aos@2.3.1/dist/aos.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css"> 
    <style>
        /* RESET DASAR */
        html, body {
            margin: 0;
            padding: 0;
            width: 100%;
            height: 100%;
        }

        body {
            background-image: linear-gradient(rgba(0, 0, 0, 0.5), rgba(0, 0, 0, 0.5)), 
                              url('/img/unnamed.jpg');
            background-size: cover;
            background-position: center;
            background-attachment: fixed;
            background-repeat: no-repeat;
            display: flex;
            flex-direction: column;
            min-height: 100vh;
        }

        /* NAVBAR HITAM SOLID & BAKU SAMBUNG */
        .navbar {
            background-color: #1a1a1a !important; /* Hitam mendekati Dashboard */
            padding: 1rem 0;
            border-bottom: 2px solid #2ecc71; /* Garis hijau pemanis */
            margin-bottom: 0 !important; /* Hapus space di bawah navbar */
        }

        /* HAPUS SPACE ANTARA NAVBAR DAN KONTEN */
        .content-wrapper {
            flex: 1 0 auto;
            margin-top: 0 !important;
            padding-top: 2rem; /* Atur jarak konten dari sini, bukan margin */
        }

        /* PERBAIKAN CARD SUPAYA TIDAK GEPENG/VERTIKAL */
        .container {
            width: 100%;
        }
        
        .row {
            display: flex;
            flex-wrap: wrap;
            margin: 0;
        }

        .card {
            backdrop-filter: blur(10px);
            background-color: rgba(255, 255, 255, 0.95) !important;
            border-radius: 15px !important;
            border: none;
            box-shadow: 0 10px 30px rgba(0,0,0,0.3) !important;
        }

        /* FOOTER */
        footer {
            background-color: #111111 !important;
            color: white;
            padding: 1.5rem 0;
            flex-shrink: 0;
        }

        /* WA Floating */
        .wa-floating {
            position: fixed;
            width: 60px; height: 60px;
            bottom: 30px; right: 30px;
            background-color: #25d366;
            color: white;
            border-radius: 50%;
            display: flex; align-items: center; justify-content: center;
            font-size: 30px; z-index: 1000;
        }
    </style>
</head>

<body>
    <%- include('../partials/navbar', { user: locals.user }) %>

    <main class="content-wrapper">
        <div class="container">
            <%- body %> 
        </div>
    </main>

    <a href="https://wa.me/6281234567890" class="wa-floating shadow-lg" target="_blank">
        <i class="fab fa-whatsapp"></i>
    </a>

    <footer class="text-center">
        <div class="container">
            <p class="mb-0 opacity-75">&copy; 2025 Sisfo Bengkel - All Rights Reserved</p>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://unpkg.com/aos@2.3.1/dist/aos.js"></script>
    <script>
        AOS.init({ duration: 800, once: true });
    </script>
</body>
</html>