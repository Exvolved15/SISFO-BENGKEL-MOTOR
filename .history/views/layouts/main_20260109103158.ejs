<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= typeof title !== 'undefined' ? title : 'Dashboard' %> | ACR Motor</title>
    
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://unpkg.com/aos@2.3.1/dist/aos.css" rel="stylesheet">
    
    <style>
        /* GLOBAL BACKGROUND (GAMBAR 1 STYLE) */
        body {
            background: linear-gradient(rgba(0, 0, 0, 0.7), rgba(0, 0, 0, 0.7)), 
                        url('https://images.unsplash.com/photo-1486006396397-30bb76aa0d94?auto=format&fit=crop&q=80');
            background-size: cover;
            background-position: center;
            background-attachment: fixed;
            background-repeat: no-repeat;
            min-height: 100vh;
            color: #ffffff;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        /* FIX UNTUK DASHBOARD: Memastikan konten tetap terbaca di atas gambar */
        .content-wrapper {
            padding-top: 80px; /* Jarak untuk navbar */
            padding-bottom: 50px;
            min-height: 90vh;
        }

        /* Glassmorphism untuk semua Card di Dashboard */
        .card {
            background: rgba(255, 255, 255, 0.95) !important;
            backdrop-filter: blur(10px);
            border: none !important;
            border-radius: 15px !important;
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
            color: #212529 !important; /* Teks dalam card tetap gelap agar terbaca */
        }

        .table {
            color: #212529 !important;
        }

        /* WhatsApp Floating */
        .wa-floating {
            position: fixed;
            width: 60px; height: 60px;
            bottom: 30px; right: 30px;
            background-color: #25d366;
            color: white;
            border-radius: 50%;
            display: flex; align-items: center; justify-content: center;
            font-size: 30px; z-index: 1000;
            transition: 0.3s;
        }
        .wa-floating:hover { transform: scale(1.1); color: white; }

        /* Navbar Styling */
        .navbar {
            background: rgba(0, 0, 0, 0.8) !important;
            backdrop-filter: blur(5px);
            border-bottom: 1px solid rgba(46, 204, 113, 0.5);
        }
    </style>
</head>
<body>
    <%- include('../partials/navbar', { user: locals.user || null }) %>

    <div class="content-wrapper">
        <div class="container-fluid px-md-4">
            <% if (locals.success_msg) { %>
                <div class="alert alert-success alert-dismissible fade show rounded-4" role="alert">
                    <%= success_msg %>
                    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                </div>
            <% } %>

            <%- body %> 
        </div>
    </div>

    <a href="https://wa.me/6281234567890" class="wa-floating shadow-lg" target="_blank">
        <i class="fab fa-whatsapp"></i>
    </a>

    <footer class="bg-black bg-opacity-50 text-white text-center py-4 mt-5">
        <p class="mb-0 small">&copy; 2025 Sisfo Bengkel ACR Motor - Makassar</p>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://unpkg.com/aos@2.3.1/dist/aos.js"></script>
    <script>
        // Inisialisasi AOS (Animasi)
        AOS.init({ duration: 800, once: true });

        // Fix untuk modal agar tidak tertutup background gambar saat dibuka
        document.addEventListener('show.bs.modal', function () {
            document.body.style.overflow = 'hidden';
        });
        document.addEventListener('hidden.bs.modal', function () {
            document.body.style.overflow = 'auto';
        });
    </script>
</body>
</html>