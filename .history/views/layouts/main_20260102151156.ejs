<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> | ACR Motor</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://unpkg.com/aos@2.3.1/dist/aos.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css"> 
    <style>
        /* RESET TOTAL UNTUK BAKU SAMBUNG */
        html, body {
            margin: 0;
            padding: 0;
            width: 100%;
            overflow-x: hidden;
        }

        body {
            background-image: linear-gradient(rgba(0, 0, 0, 0.6), rgba(0, 0, 0, 0.6)), 
                              url('/img/unnamed.jpg');
            background-size: cover;
            background-position: center;
            background-attachment: fixed;
            background-repeat: no-repeat;
            min-height: 100vh;
            /* Jangan pakai display: flex di sini karena merusak grid login */
        }

        /* NAVBAR HITAM SOLID & RAPAT */
        .navbar {
            background-color: #1a1a1a !important; 
            border-bottom: 2px solid #2ecc71;
            margin: 0 !important; /* Hapus space */
            z-index: 1050;
        }

        .content-wrapper {
            margin-top: 0 !important; /* Rapat dengan navbar */
            padding: 0;
            width: 100%;
        }

        /* WA Floating */
        .wa-floating {
            position: fixed;
            width: 60px; height: 60px;
            bottom: 30px; right: 30px;
            background-color: #25d366;
            color: white;
            border-radius: 50%;
            display: flex; align-items: center; justify-content: center;
            font-size: 30px; z-index: 1000;
            text-decoration: none;
        }

        footer {
            background-color: #111111 !important;
            color: white;
            margin-top: 5rem;
        }
    </style>
</head>
<body>
    <%- include('../partials/navbar', { user: locals.user }) %>

    <main class="content-wrapper">
        <%- body %> 
    </main>

    <a href="https://wa.me/6281234567890" class="wa-floating shadow-lg" target="_blank">
        <i class="fab fa-whatsapp"></i>
    </a>

    <footer class="py-4 text-center">
        <p class="mb-0 opacity-75">&copy; 2025 Sisfo Bengkel - All Rights Reserved</p>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://unpkg.com/aos@2.3.1/dist/aos.js"></script>
    <script>
        AOS.init({ duration: 800, once: true });
    </script>
</body>
</html>