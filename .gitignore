# Dependency folders (sangat berat, tidak perlu di-upload)
node_modules/
jspm_packages/

# Environment variables (Berisi JWT_SECRET, DB_URL, Firebase Keys)
# SANGAT PENTING: Jangan hapus baris ini demi keamanan
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Firebase & Service Account Keys
# Menyembunyikan file JSON kredensial admin firebase
*.json
!package.json
!package-lock.json

# Uploads & User Data (File yang diupload saat testing tidak perlu ikut)
public/uploads/
uploads/

# Abaikan foto profil hasil upload, tapi biarkan aset utama tetap ikut
public/img/profile-*
public/img/unnamed.jpg_backup

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# OS Metadata (Sampah sistem operasi)
.DS_Store
Thumbs.db
desktop.ini

# IDE/Editor folders
.vscode/
.idea/
.history/    <-- TAMBAHKAN INI (Mengabaikan Local History VS Code)
*.swp
*.swo

# Build/Dist folders
dist/
build/